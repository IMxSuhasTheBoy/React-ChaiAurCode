import React from "react"; // core lib
import ReactDOM from "react-dom/client"; // addtional implimentation on web

import App from "./App";

const root = ReactDOM.createRoot(document.getElementById("root")); // virtual DOM
root.render(<App />);
